# Copy this file to CTFd/.env before starting Docker Compose
# and set strong credentials.

# MariaDB credentials used by CTFd/docker-compose.yml
MARIADB_ROOT_PASSWORD=change_this_root_password
MARIADB_PASSWORD=change_this_ctfd_db_password
